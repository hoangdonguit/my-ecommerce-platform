apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: inventory-api-cpu-stress
  namespace: default
spec:
  mode: one
  selector:
    namespaces:
      - default
    labelSelectors:
      app: inventory-api
  stressors:
    cpu:
      workers: 1
      load: 100
  duration: "5m"
